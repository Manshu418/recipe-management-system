<!-- views/index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recipe Management System</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
<header>
  <h1>🍽️ Recipe Management</h1>

  <div class="header-right">
    <% if (session && session.userId) { %>
      <a href="/dashboard" class="btn btn-secondary">Dashboard</a>
      <a href="/" class="btn btn-secondary">Recipes</a>
      <a href="/add" class="btn btn-primary">+ Add Recipe</a>
      <a href="/logout" class="btn btn-danger">Logout</a>
    <% } else { %>
      <a href="/login" class="btn btn-secondary">Login</a>
      <a href="/register" class="btn btn-primary">Register</a>
    <% } %>
  </div>
</header>



    <main>
      <!-- Default Recipes Section -->
      <div class="collapsible-section">
        <div class="section-toggle" onclick="toggleSection('default-section')">
          <div class="toggle-header">
            <h2>📚 Learn from Default Recipes</h2>
            <span class="recipe-count"><%= defaultRecipes.length %> recipes</span>
          </div>
          <span class="toggle-icon" id="default-icon">▼</span>
        </div>
        
        <div class="section-content" id="default-section">
          <% if (defaultRecipes.length === 0) { %>
            <div class="empty-section">
              <p>No default recipes available</p>
            </div>
          <% } else { %>
            <div class="recipe-grid">
              <% defaultRecipes.forEach(recipe => { %>
                <div class="recipe-card default-recipe">
                  <div class="recipe-badge">Default Recipe</div>
                  <div class="recipe-header">
                    <h3><%= recipe.name %></h3>
                    <span class="category"><%= recipe.category || 'Uncategorized' %></span>
                  </div>
                  
                  <div class="recipe-meta">
                    <div class="meta-item">
                      <span class="icon">⏱️</span>
                      <span><%= recipe.prep_time || 0 %> + <%= recipe.cook_time || 0 %> min</span>
                    </div>
                    <div class="meta-item">
                      <span class="icon">🍽️</span>
                      <span><%= recipe.servings || 'N/A' %> servings</span>
                    </div>
                  </div>
                  
                    <div class="recipe-actions">
                    <a href="/recipe/<%= recipe.id %>" class="btn btn-secondary">View Recipe</a>
                    <form action="/delete/<%= recipe.id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this default recipe?');">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                    </div>
                </div>
              <% }); %>
            </div>
          <% } %>
        </div>
      </div>

      <!-- Divider -->
      <div class="section-divider"></div>

      <!-- User Recipes Section -->
      <div class="collapsible-section">
        <div class="section-toggle" onclick="toggleSection('user-section')">
          <div class="toggle-header">
            <h2>👤 My Personal Recipes</h2>
            <span class="recipe-count"><%= userRecipes.length %> recipes</span>
          </div>
          <span class="toggle-icon" id="user-icon">▼</span>
        </div>
        
        <div class="section-content" id="user-section">
          <% if (userRecipes.length === 0) { %>
            <div class="empty-state">
              <div class="empty-icon">📝</div>
              <h3>No personal recipes yet!</h3>
              <p>Start building your collection by adding your first recipe.</p>
              <a href="/add" class="btn btn-primary">Add Your First Recipe</a>
            </div>
          <% } else { %>
            <div class="recipe-grid">
              <% userRecipes.forEach(recipe => { %>
                <div class="recipe-card user-recipe">
                  <div class="recipe-badge user-badge">My Recipe</div>
                  <div class="recipe-header">
                    <h3><%= recipe.name %></h3>
                    <span class="category"><%= recipe.category || 'Uncategorized' %></span>
                  </div>
                  
                  <div class="recipe-meta">
                    <div class="meta-item">
                      <span class="icon">⏱️</span>
                      <span><%= recipe.prep_time || 0 %> + <%= recipe.cook_time || 0 %> min</span>
                    </div>
                    <div class="meta-item">
                      <span class="icon">🍽️</span>
                      <span><%= recipe.servings || 'N/A' %> servings</span>
                    </div>
                  </div>
                  
                  <div class="recipe-actions">
                    <a href="/recipe/<%= recipe.id %>" class="btn btn-secondary">View Recipe</a>
                    <form action="/delete/<%= recipe.id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this recipe?');">
                      <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                  </div>
                </div>
              <% }); %>
            </div>
          <% } %>
        </div>
      </div>
    </main>
  </div>

  <script>
    function toggleSection(sectionId) {
      const section = document.getElementById(sectionId);
      const icon = document.getElementById(sectionId.replace('-section', '-icon'));
      
      if (section.style.display === 'none' || section.style.display === '') {
        section.style.display = 'block';
        icon.textContent = '▲';
      } else {
        section.style.display = 'none';
        icon.textContent = '▼';
      }
    }

    // Initially hide both sections
    window.onload = function() {
      document.getElementById('default-section').style.display = 'none';
      document.getElementById('user-section').style.display = 'none';
    };
  </script>
</body>
</html>